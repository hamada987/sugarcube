import "../styles.css";

import React from "react";

import Layout from "../components/layout";

const Plugins = () => {
  return (
    <Layout>
      <h1>Plugins.</h1>
    </Layout>
  );
};

export default Plugins;
